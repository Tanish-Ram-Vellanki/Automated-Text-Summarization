# Automated-Text-Summarization
Automated text summarization using a scratch custom Transformer encoder–decoder architecture with self-attention and cross-attention, trained two separate models on dialogue and news datasets.
